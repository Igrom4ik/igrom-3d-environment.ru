# План работ: Этап 2 — Производительность, Безопасность и Тесты

Мы успешно завершили **Этап 1 (Данные и Структура)**: внедрили единый `ContentService`, стандартизировали хранение контента, добавили валидацию (Zod) и отрефакторили API и компоненты.

Теперь необходимо заняться оптимизацией и надежностью системы.

## 1. 🚀 Производительность (Caching)
**Цель:** Убрать лишние операции чтения с диска при каждом запросе.
- **Внедрение In-Memory Cache в `ContentService`:**
  - Добавить `Map<slug, { data, lastModified }>` для альбомов, проектов и постов.
  - При запросе проверять дату изменения файла (`fs.statSync`). Если файл не менялся — отдавать из кэша.
  - Это значительно ускорит работу API и SSG генерации.

## 2. 🛡️ Безопасность
**Цель:** Защитить приложение от базовых уязвимостей.
- **Защита от Path Traversal:**
  - В `ContentService` добавить проверку, что итоговый путь к файлу действительно находится внутри разрешенной директории (`src/content/...`).
- **Rate Limiting (Лимит запросов):**
  - Добавить простой middleware или утилиту для ограничения частоты запросов к API (например, для `/api/portfolio/save`), чтобы предотвратить спам/abuse.

## 3. 🧪 Тестирование (Unit Tests)
**Цель:** Гарантировать, что `ContentService` работает корректно и не сломается при изменениях.
- **Настройка Vitest (или Jest):**
  - Установить тестовый фреймворк.
- **Написание тестов для `ContentService`:**
  - Тест: Чтение существующего альбома (Keystatic и FS fallback).
  - Тест: Обработка несуществующего слага (должен вернуть null).
  - Тест: Валидация данных (Zod schema).

## 4. 🧹 Очистка и Доработка
- **Удаление старого кода:**
  - Проверить и удалить неиспользуемые функции в `src/utils/utils.ts` и `src/utils/reader.ts`, которые теперь дублируются в `ContentService`.
- **Review "TODOs":** Пройтись по коду и закрыть мелкие "хвосты", если они остались.

---

**Предлагаю начать с пункта 1 (Кэширование) и 2 (Безопасность FS), так как это быстрые и важные улучшения для `ContentService`.** Приступаем?